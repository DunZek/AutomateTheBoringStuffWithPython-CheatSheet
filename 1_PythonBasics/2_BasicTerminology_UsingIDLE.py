"""
Expressions:
    >>> 2 + 2
    - Expression = Values + Operators.
    - Operators evaluate values to a single number.
    - Order of Operations: BEDMAS

Data Type:
    - Every value belongs to only one kind of datatype.
    - A datatype is a category of values.
    - strings, ints, floats, bools

Variables:
    - Stores values.
    - can take in strings, ints, floats, bools, object instances,
    functions returns, or even mathematical expressions.

    NOTE: If it evaluate, then Python considers it as an expression. Otherwise,
    it is a statement.

    NOTE: "spam", "eggs", "bacon", and "ham" are all used as placeholder names
    for variables because of the "spam comedy sketch from Monty Python".
"""